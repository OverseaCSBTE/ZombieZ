#if defined _tools_b_included
  #endinput
#endif
#define _tools_b_included

#include <tools/tools_version>

// Byte
native get_controller(Index, cIndex=0);
native set_controller(Index, cIndex=0, Controller)
