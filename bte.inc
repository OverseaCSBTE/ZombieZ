#define BTE_STRING 1
#define BTE_FLOAT 2
#define BTE_INT 3

native BTE_GetPrivateProfile(lpAppName[], lpKeyName[], lpDefault[], lpFileName[], iType, any:...);
native BTE_CheckValid()
//native UTIL_TutorText(id, str[], color, Float:time)

native AnsiToUTF8(in[], out[], len);
